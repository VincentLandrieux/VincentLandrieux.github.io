{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch src/css:./",
    "build": "esbuild ./src/scripts/script.js ./src/scripts/work.js --outdir=./ --bundle --minify --sourcemap && sass ./src/css:./"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.12.25",
    "sass": "^1.38.2"
  }
}
